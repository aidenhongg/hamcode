# Core ML stack — tested on RTX 4090 (Ada, sm_89) Runpod.
#
# IMPORTANT: install torch from the cu124 index, NOT cu128.  PyTorch's cu128
# index has started shipping +cu130 wheels at the latest version, which need
# NVIDIA driver 12.8+; many current Runpod 4090 templates only ship driver
# 12.4. cu124 wheels work on driver 12.4+ which covers all current templates.
#
#   pip install --force-reinstall --index-url https://download.pytorch.org/whl/cu124 torch
#
# If the base image ships torchvision/torchaudio, uninstall them to avoid
# transformers' lazy-vision import exploding:
#   pip uninstall -y torchvision torchaudio
torch>=2.7.0
transformers>=4.45.0,<5.0.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=2.20.0
safetensors>=0.4.5

# Tokenization / parsing — per-language tree-sitter packages (Python 3.13 abi3 wheels).
# We previously used tree_sitter_languages but it doesn't ship 3.13 wheels.
tree_sitter>=0.23,<1.0
tree-sitter-python>=0.21
tree-sitter-java>=0.21
tree-sitter-cpp>=0.22
tree-sitter-c>=0.22
tree-sitter-c-sharp>=0.21
tree-sitter-go>=0.22
tree-sitter-javascript>=0.22
tree-sitter-typescript>=0.21
tree-sitter-php>=0.22
tree-sitter-ruby>=0.21
tree-sitter-rust>=0.21
tree-sitter-swift>=0.0.1
markdown-it-py>=3.0.0

# Data
pandas>=2.2.0
pyarrow>=16.0.0
numpy>=1.26.0
datasketch>=1.6.5
scikit-learn>=1.4.0

# Utilities
pyyaml>=6.0
tqdm>=4.66.0
click>=8.1.0
joblib>=1.4.0

# Logging (optional; code falls back to JSONL/TensorBoard if wandb missing)
wandb>=0.17.0; python_version >= "3.9"
tensorboard>=2.15.0

# Plotting + HP search
matplotlib>=3.8.0
optuna>=3.6.0

# Stacking heads (optional)
lightgbm>=4.3.0
xgboost>=2.0.0
